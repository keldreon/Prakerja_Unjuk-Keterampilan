import 'package:flutter/material.dart';

class Pallete {
  static const Color backgroundColor = Colors.white;
  static const Color searchBarColor = Color.fromRGBO(32, 35, 39, 1);
  static const Color selectedIcon = Color.fromRGBO(0, 33, 64, 1);
  static const Color unSelectedIcon = Color.fromRGBO(67, 91, 113, 1);
  static const Color greenColor = Color.fromRGBO(13, 152, 106, 1);
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
}
